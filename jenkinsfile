pipeline {
    agent any

    stages {
        stage('Check SonarQube Connection') {
            steps {
                script {
                    def response = sh(script: "curl -IsS -u squ_d02ae556436a895ba202b8adeb89a0844d253c4e: http://localhost:9000/api/system/status", returnStatus: true)
                    if (response == 0) {
                        echo "Conexión exitosa a SonarQube en http://localhost:9000/"
                    } else {
                        error "No se pudo conectar a SonarQube en http://localhost:9000/"
                    }
                }
            }
        }
    }
}

